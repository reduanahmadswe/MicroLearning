# 🚀 Quick Start Commands

## First Time Setup
```powershell
cd frontend
npm install
```

## Development
```powershell
# Start dev server
npm run dev

# Start with type checking
npm run type-check; npm run dev
```

## Build
```powershell
# Build for production
npm run build

# Run production build
npm start
```

## Linting & Type Checking
```powershell
# Check types
npm run type-check

# Lint code
npm run lint

# Format code (if you add prettier script)
npm run format
```

## Common Tasks
```powershell
# Clear cache and reinstall
rm -rf node_modules .next
npm install

# Update all dependencies
npm update

# Check for outdated packages
npm outdated
```

## 🎯 First Time? Run This:
```powershell
cd frontend
npm install
npm run dev
```

Then open: http://localhost:3000 🎉
